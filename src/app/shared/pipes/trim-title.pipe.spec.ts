import { TrimTitlePipe } from './trim-title.pipe';

describe('TrimTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new TrimTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
